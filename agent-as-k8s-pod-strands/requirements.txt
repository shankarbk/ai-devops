# =============================================================================
# requirements.txt — DevOps Agent dependencies
#
# PACKAGE NAME CORRECTIONS vs original guide:
#   ✓ bedrock-agentcore      — the real PyPI package name (not amazon-bedrock-agentcore)
#   ✓ strands-agents         — correct name (was strands-agents-tools, which doesn't exist)
#   ✓ kubernetes             — Python client for K8s API (unchanged)
#   ✓ boto3                  — AWS SDK (unchanged)
#
# PINNED vs UNPINNED:
#   Pinning to exact versions makes builds reproducible — same image every build.
#   Use `pip list --outdated` periodically to check for updates.
# =============================================================================

# Amazon Bedrock AgentCore SDK — wraps your agent in an HTTP server
# that AgentCore Runtime (or EKS) can invoke
bedrock-agentcore>=1.4.0

# AWS Strands Agents SDK — the @tool decorator and Agent class
strands-agents>=0.1.0

# AWS SDK — boto3 for Bedrock InvokeModel calls
boto3>=1.34.0

# Kubernetes Python client — used by our 4 agent tools (get_pod_logs, restart_pod, etc.)
kubernetes>=29.0.0

# Pydantic v2 — data validation, required by strands-agents
pydantic>=2.0.0

# Uvicorn — ASGI server that runs the BedrockAgentCoreApp (Starlette-based)
uvicorn>=0.29.0
